OSCA WEBPAGE

notes:
-- "Templates" folder inside the OSCA_WEB_APP is the pages where as "Templates" outside is the mainframe of the whole system
-- IF you want to "python manage.py runserver" you have to first activate the virtual environment using "oscavenv/Scripts/Activate" on the terminal
-- IF you want to deactivate the virtual environment use the "oscavenv/Scripts/Deactivate" code on the terminal 
-- List the Changes in "Updates" folder before you commit the code and add a file and inside write the date and the updates made
-- Backup starts every saturday @12AM weekly so be sure to update your codes before then.
-- Update the lead programmer if there were some issues about the code immidiately if you can't resolve it
